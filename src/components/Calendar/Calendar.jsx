import s from './Calendar.module.css';

const Calendar = () => {
    return (
        <div className={s.calendar + ' buttons2'}>
            Календарь
        </div>
    );
};

export default Calendar;